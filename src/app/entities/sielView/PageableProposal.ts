import { ActivityProposalView } from "./ActivityProposalView";
import { PageDetails } from "./PageDetails";

export class PageableProposal{
    activities : Array<ActivityProposalView> =[];

    page : PageDetails = new PageDetails;
    
}