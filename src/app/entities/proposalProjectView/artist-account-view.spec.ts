import { ArtistAccountView } from './artist-account-view';

describe('ArtistAccountView', () => {
  it('should create an instance', () => {
    expect(new ArtistAccountView()).toBeTruthy();
  });
});
